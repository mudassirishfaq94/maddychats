/**
 * /app/chats index — the empty selection state renders inside ChatsLayout,
 * so this page contributes nothing (the layout knows when no id is active).
 */
export default function ChatsIndexPage() {
  return null;
}
